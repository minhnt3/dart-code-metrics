name: dart_code_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 4.8.1
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  dart_code_metrics:
    path: ../../../dart-code-metrics

dev_dependencies:
  lints: ^1.0.1
